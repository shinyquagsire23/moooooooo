Clone `https://github.com/shinyquagsire23/unicorn` to `unicorn/`

Compile unicorn
`UNICORN_QEMU_FLAGS="--python=python2" UNICORN_ARCHS="arm aarch64" ./make.sh`

Compile
`make`

Bring your own kernel `0_saltedkernel_90000000.bin`.
